---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<h2 class:list={["font-heading text-3xl sm:text-4xl lg:text-5xl font-bold text-center text-ink uppercase", className]}>
  <slot />
</h2>
